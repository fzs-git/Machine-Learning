# 特征类型
## 数值型特征
### 1. 对数变换
对于数值较大的特征项可以取对数，对数的良好性质：
1. 单调递增，不改变特征值的相对关系
2. 压缩变量尺度
3. 可以作为消除异方差的一种手段
#### 引申-异方差：
若线性回归模型存在异方差性，则用传统的最小二乘法估计模型，得到的参数估计量不是有效估计量，甚至也不是渐近有效的估计量；此时也无法对模型参数进行有关显著性检验。对存在异方差性的模型可以采用加权最小二乘法进行估计。
线性回归模型：
